# M01.2 negative boundary fixtures

Estos archivos son fixtures deliberadamente inválidos y **no** forman parte del `tsconfig` de compilación. Las pruebas de arquitectura reproducen sus patrones y exigen que la política falle cerrada ante imports de engines desde `domain`, deep imports de workspace y cruces relativos entre packages.
