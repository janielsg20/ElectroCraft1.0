{
  "name": "electrocraft-m00-4-studio-db",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@electric-sql/pglite": "0.5.5",
    "drizzle-orm": "0.45.2"
  },
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "typecheck": "node scripts/typecheck.mjs",
    "test": "node --test test/*.test.mjs",
    "integration": "node scripts/integration.mjs",
    "browser-contract": "node scripts/browser-contract.mjs",
    "build": "node scripts/build.mjs",
    "verify": "npm run verify:structural && npm run two-tab-runtime && npm run closure-gate",
    "verify:structural": "npm run lint && npm run typecheck && npm test && npm run integration && npm run browser-contract && npm run build",
    "two-tab-runtime": "node scripts/two-tab-runtime.mjs",
    "closure-gate": "node scripts/closure-gate.mjs"
  },
  "devDependencies": {
    "playwright": "1.62.1",
    "vite": "8.2.1"
  }
}
