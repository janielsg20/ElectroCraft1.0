import { execFileSync } from 'node:child_process'; import { existsSync, readFileSync } from 'node:fs'; import { resolve } from 'node:path';
const cwd=resolve('capacitor-poc'); execFileSync('npm',['install','--ignore-scripts','--no-audit','--no-fund'],{cwd,stdio:'inherit'}); execFileSync('npx',['cap','add','android'],{cwd,stdio:'inherit'}); execFileSync('npx',['cap','sync','android'],{cwd,stdio:'inherit'}); if(!existsSync(resolve(cwd,'android/app/src/main/AndroidManifest.xml'))) throw new Error('Capacitor Android project missing'); const config=JSON.parse(readFileSync(resolve(cwd,'capacitor.config.json'),'utf8')); if(config.webDir!=='www') throw new Error('Unexpected webDir'); console.log('PASS_REAL_CAPACITOR_SYNC android project generated and synced');
