import http from 'node:http'; import { readFile, stat } from 'node:fs/promises'; import { extname, join, normalize } from 'node:path';
const root='dist'; const mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.txt':'text/plain; charset=utf-8'};
http.createServer(async(req,res)=>{ try{ let path=normalize(req.url==='/'?'/index.html':req.url).replace(/^\/+/, ''); const full=join(root,path); const s=await stat(full); if(!s.isFile()) throw new Error('not file'); res.setHeader('content-type',mime[extname(full)]||'application/octet-stream'); res.end(await readFile(full)); }catch{ res.statusCode=404; res.end('Not found'); } }).listen(Number(process.env.PORT||4173),()=>console.log(`POC exportación http://127.0.0.1:${process.env.PORT||4173}`));
