import {cp,mkdir,rm,writeFile} from 'node:fs/promises';const out=new URL('../dist/',import.meta.url);await rm(out,{recursive:true,force:true});await mkdir(out,{recursive:true});await cp(new URL('../public/',import.meta.url),out,{recursive:true});await mkdir(new URL('./src/',out),{recursive:true});await cp(new URL('../src/',import.meta.url),new URL('./src/',out),{recursive:true});await writeFile(new URL('BUILD.txt',out),'ElectroCraft M00.9 data-source POC static harness\n');console.log('PASS_BUILD dist/ created');
