import {readdir,stat} from 'node:fs/promises';import {join} from 'node:path';let bytes=0,count=0;for(const root of ['src','public'])await walk(root);console.log(JSON.stringify({pocOwnSourceBytes:bytes,pocOwnSourceFiles:count,sqlDriverDependencies:0,queryCacheDependencies:0,openApiParserDependency:'@scalar/openapi-parser@0.28.11'}));if(bytes>120000)throw new Error('POC own-source budget exceeded');
async function walk(dir){for(const e of await readdir(dir,{withFileTypes:true})){const p=join(dir,e.name);if(e.isDirectory())await walk(p);else{bytes+=(await stat(p)).size;count++;}}}
