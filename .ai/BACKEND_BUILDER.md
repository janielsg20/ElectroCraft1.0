# ADMINISTRATION BUILDER — compatibility filename

Visible:
`App > Administración`.

Refine Core:
admin CRUD/data lifecycle.

TanStack Table:
table behavior.

RHF/Zod:
record forms.

Puck:
visual composition of admin screens, using the same ElectroCraftDocument model.

Data:
same DataSource/Query services as the rest of the app.

Views:
Dashboard, List, Create, Edit, Detail, Kanban, Calendar, Custom.

Do not use Refine as the general runtime for normal app Screens.
