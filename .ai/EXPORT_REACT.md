# WEB / REACT / PWA EXPORT — Core

Owner:
F21.

Source:
validated ElectroCraftExportIR.

Generate:
React source, routing, screens/components, theme/tokens, Data Source adapters, queries, state, forms, actions, auth, media/assets, optional Administration, docs/env example.

Runtime:
independent from Puck.

Routing:
target adapter from ElectroCraft Navigation.

Dependency pruning:
RuntimeDependencyManifest.

PWA:
manifest/offline/service worker as selected.

Build fixture:
install -> typecheck -> build -> artifact verification.
