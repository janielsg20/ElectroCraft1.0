# HANDOFF — ElectroCraft

## Current
F01 / M01.6 — Documentar conventions — `ACTIVE`.

## Siguiente acción exacta
1. Ejecutar/validar `npm run test:docs` y `npm run build:docs`.
2. Ejecutar pipeline completo (`lint`, `typecheck`, `test`, `build`, Playwright/CI).
3. Registrar artifact/marker real de M01.6.
4. Si todo queda verde, cerrar Gate F01.
5. Activar F02 / M02.1 — Definir `ElectroCraftProjectDefinition` y `ElectroCraftDocument`.

## Read set
`AGENTS → .ai/README → RULES → MEMORY → STATE → TRACKING → HANDOFF → .ai/microphases/M01_6.md`.

No reabrir M01.5 salvo regresión reproducible.
