# F05 — Screen Composer con Puck

Estado de fase: `COMPLETADA / GREEN`.

## Objetivo
Integrar Puck como único Screen Composer del Studio sin persistir internals del engine ni duplicar su editor.

## Cierre

F05 cerró mediante PR `#60`, squash `a81ca149c17391b9fe77aaaf57b125d229320173` y `ElectroCraft Base CI` run `33101434587` (#742) en GREEN.

## Cerrado en esta fase
- M05.1 — PuckAdapter y component mapping: `COMPLETADA / GREEN`.
- M05.2 — Components/Outline/Preview/Fields: `COMPLETADA / GREEN`.
- M05.3 — Nested Slots, permissions y Puck data migration: `COMPLETADA / GREEN`.
- M05.4 — Sincronización Puck actions -> ElectroCraftDocument: `COMPLETADA / GREEN`.
- M05.5 — Puck visual history: `COMPLETADA / GREEN`.
- M05.6 — Text/RichText inline editing: `COMPLETADA / GREEN`.
- M05.7 — Extensiones de Palette/Outline solo necesarias: `COMPLETADA / GREEN`.
- M05.8 — Editor core E2E: `COMPLETADA / GREEN`.

## Reglas
- Puck se consume detrás de `@electrocraft/editor-puck`.
- `ElectroCraftDocument` es el modelo portable; AppState/history/zones no se persisten.
- Usar Composition/Slots/Fields/Outline/Preview públicos de Puck.
- `Puck.Components` y `Config.categories` poseen la lista/categorías; Electro solo añade descubrimiento opcional.
- `Puck.Outline` permanece directo; no usar overrides/plugins experimentales como dependencia release-critical.
- Lock se traduce a permisos públicos Puck; hidden/visibility requiere owner canónico Electro y no se inventa en el adapter.
- Puck `text`/`richtext` + `contentEditable` posee la edición inline; Tiptap es el único engine RichText.
- Diagnostics unknown/unsupported permanecen visibles y reparables, pero no insertables desde la Palette.
- Editor history y Project Revisions son historias separadas.
- El E2E core debe atravesar proyecto real, Palette, Outline, Fields, history y storage; no se acepta fixture que sustituya el runtime visible.
- Base CI es el gate transversal por defecto.

Referencia de cierre: `.ai/evidence/F05/CLOSURE_2026-08-27.md`.
