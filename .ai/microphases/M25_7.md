# M25.7 — Installer, dependencias, migraciones y paquete desplegable LAMP

**Fase propietaria:** F25 — Exportación LAMP con Slim/PDO

## Por qué existe
Una exportación de primera clase debe poder instalarse limpiamente, no entregar una carpeta de PHP sin procedimiento reproducible.

## Resultado exacto
Al cerrar esta microfase, **Installer, dependencias, migraciones y paquete desplegable LAMP** funciona de forma observable dentro del target correspondiente y pasa su gate. No se acepta un archivo generado sin validación ni una opción visual sin compiler/runtime real.

## Engine owner
`Composer + generated installer/migration runner`

## Responsabilidad ElectroCraft
ElectroCraft implementa únicamente el mapping desde modelos canónicos, la UX española, configuración, validación, diagnósticos, seguridad, compilación y evidencia. El framework/SDK target conserva routing/runtime/API behavior que ya ofrece.

## Ubicación de código obligatoria
- `packages/export-lamp/`
- `packages/export-core/`
- `fixtures/lamp/`

## Ubicación exacta en la app
`Publicar > Exportar > LAMP > Instalación`.

## Diseño visual exacto
Checklist: PHP, extensiones, Composer vendor, DB config, migrations, writable dirs, base URL. Option `Incluir dependencias`.

## Texto visible en español
Instalación; Dependencias; Incluir vendor; Variables de entorno; Ejecutar migraciones; Verificar servidor.

## Ayuda contextual
Usa HelpRegistry `help.export.lamp.install`. El H1 usa `CircleHelp`; las opciones técnicas muestran Info solo cuando explican una decisión o requisito del target.

## Precondiciones
- Gate de la fase anterior satisfecho.
- ExportIR/TargetRegistry/Capability Analyzer disponibles.
- Tests existentes verdes antes de modificar el target.
- No existe un exporter paralelo para la misma responsabilidad.

## Implementación paso por paso
1. Generate composer.json with exact required packages.
2. Generate .env.example names only.
3. Generate install/migrate CLI or safe web installer policy; prefer CLI when available.
4. When `includeVendor`, run composer install production and package vendor after security/license check.
5. Generate storage/public permissions instructions.
6. Create deployable ZIP plus checksum.
7. Generate README Spanish/technical install steps.
8. Test install from empty directory/database.
9. Compila Routes a Slim, Data a PDO/MySQL y Screens a HTML/PHP.
10. Usa Slim/PSR-7/CSRF/PDO; no infra HTTP/DB propietaria.
11. Genera Composer/migrations/env example.
12. Ejecuta clean install + HTTP/security tests.
13. Empaqueta ZIP y verifica checksum.

## Artefactos obligatorios al cerrar
- `composer.json`
- `installer/migration command`
- `deploy ZIP`
- `README`
- `install report`

## Estados UI obligatorios
- No analizado.
- Analizando…
- Compatible / Compatible con adaptaciones / Advertencias / Bloqueado.
- Generando…
- Verificando…
- Listo.
- Error con recuperación.

## Responsive
- Desktop: lista de targets 240–260, config central, compatibilidad 320.
- Tablet: targets en rail/Sheet; configuración central.
- Mobile: selector de destino -> configuración -> compatibilidad -> generar -> resultado.

## Accesibilidad
- teclado completo;
- focus-visible;
- labels persistentes;
- estados no dependen solo de color;
- icon-only con aria-label;
- errores anunciables;
- progress/status accesible.

## Pruebas obligatorias
1. composer validate/install.
2. Empty DB install.
3. Package checksum.
4. No secrets.
5. Reinstall/upgrade.
90. lint.
91. typecheck.
92. test.
93. build.

## Prohibido
- No embed real .env.
- No require Composer on production if deployable ZIP includes vendor.

## Evidencia TRACKING
- archivos generados/modificados;
- target descriptor/compiler utilizado;
- capability report;
- comandos reales de validación;
- paths/checksums de artifacts cuando existan;
- errores/limitaciones;
- siguiente microfase exacta.
