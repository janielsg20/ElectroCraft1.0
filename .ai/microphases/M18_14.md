# M18.14 — AI QA completo: Gemini, español, seguridad, responsive y artifacts

**Fase propietaria:** F18 — Generar con IA — Gemini

## Por qué existe
La fase no puede terminar con demos parciales; debe probar el flujo completo desde prompt hasta artifact aplicado.

## Resultado exacto
Al cerrar esta microfase, **AI QA completo: Gemini, español, seguridad, responsive y artifacts** está implementada de extremo a extremo para su alcance y produce evidencia real.

## Engine owner
`All AI engines/adapters + Playwright/test fixtures`

## Responsabilidad ElectroCraft
ElectroCraft controla contratos, contexto, UX en español, permisos, validación, borradores, diff y Apply. El proveedor de IA nunca muta el proyecto directamente.

## Ubicación de código obligatoria
- `packages/ai/`
- `apps/studio/src/features/ai/`
- `apps/studio/src/components/ai-elements/`

## Ubicación exacta en la app
Toda la ruta `Generar con IA`, Settings IA, Extensiones Borradores y contextual actions.

## Diseño visual exacto
Ejecuta matriz Desktop/Laptop/Tablet/Mobile, offline/provider-error y multiple artifact kinds.

## Texto y ayuda
Todo texto visible usa i18n español. El título de la sección lleva `CircleHelp`; las opciones técnicas no obvias usan `Info` con HelpRegistry. Nunca mostrar raw provider errors como texto principal.

## Texto visible en español
Generar con IA; Crear; Historial; Contexto; Opciones; Validación; Generar; Aplicar cambios; Guardar como reutilizable.

## Ayuda contextual
Usa HelpRegistry `help.ai`. Si esta microfase introduce un concepto nuevo, añade/actualiza su HelpDescriptor en español. La ayuda crítica no puede vivir solo en un tooltip.

## Precondiciones
- F17 COMPLETADA: los tipos de artifact reutilizable y ElectroCraftExtensionPackage existen.
- F00 POC Gemini aprobado.
- i18n/HelpRegistry/AppShell disponibles.
- provider secrets fuera del cliente.

## Implementación paso por paso
1. Crea `MockAIModel`/provider fixture usando interfaces de AI SDK para CI determinista sin red.
2. E2E: screen draft -> preview -> diff -> apply -> revision.
3. E2E: navigation + data-model/query/form bundle.
4. E2E: declarative extension -> save draft -> install through Extensions flow.
5. E2E: image -> stage -> apply to Media.
6. Security: unknown tool, invalid input, prompt injection, secret scan, direct Apply impossible, generated-code quarantine.
7. Spanish: cero labels inglesas en AI route/settings/help.
8. Responsive: 320/375/768/1024/1280/1440.
9. Offline: history/drafts work; Generate disabled.
10. Run real Gemini smoke only through secure gateway/test credentials; otherwise mark SKIPPED with evidence.
11. Verify no duplicated `AI SDK model/provider interface`/Gemini full adapter remains beside AI SDK.
12. Finalize AI capability/dependency matrix.
13. Define request/draft schema antes de prompt/UI.
14. Usa AI SDK para model/tools/structured output y AI Elements para UI estándar.
15. Todas las tools escriben Draft o leen contexto sanitizado.
16. Valida/Preview/Diff antes de Apply.
17. Aplica solo por AIApplyService transaccional.
18. Prueba mock determinista + security + Spanish.

## Artefactos obligatorios al cerrar
- `AI Playwright suite`
- `provider mock fixtures`
- `security report`
- `AI_E2E_REPORT.md`
- `screenshots/evidence`

## Estados UI obligatorios
- inicial;
- proveedor no configurado;
- sin conexión;
- preparando contexto;
- generando;
- esperando herramienta;
- validando;
- borrador válido;
- con advertencias;
- bloqueado;
- cancelado;
- error;
- aplicado;
- descartado.

## Responsive y accesibilidad
- Desktop: Context 288 + Workbench flex + Inspector 320.
- Tablet: Context/Inspector como Sheets.
- Mobile: wizard secuencial; no tres paneles estrechos.
- Message log keyboard-focusable y `aria-busy` durante streaming.
- botones icon-only con aria-label;
- cambios/errores no dependen solo de color.

## Pruebas obligatorias
1. All artifact workflows.
2. Security/adversarial.
3. Spanish lint.
4. Responsive.
5. Offline.
6. Real smoke if credentials.
90. lint.
91. typecheck.
92. test.
93. build.

## Prohibido
- No mark phase complete from a single Gemini happy-path demo.
- No network credential required for deterministic CI.

## Evidencia TRACKING
- archivos modificados;
- provider/model profile usado;
- fixtures sanitizadas;
- E2E/screenshots;
- tests/build;
- próxima microfase.
