# EXPORT LOCAL PACKAGE — Core

Purpose:
portable ElectroCraft project package.

Includes:
manifest, project objects, selected content/internal data, media assets, migrations, compatibility metadata, professionalStudio metadata when enabled.

Excludes:
secrets, workspace preferences by default, Puck/Rete history, AI prompt/history by default, runtime caches.

Import validates:
version, manifest, checksums, paths and migrations.
