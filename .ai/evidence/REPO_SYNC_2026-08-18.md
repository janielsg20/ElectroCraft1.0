# Repository synchronization evidence — 2026-08-18

## Scope
Repository: `janielsg20/ElectroCraft1.0`.
Branch: `main`.
Pre-cleanup head: `3fba1a4c0033b346b78969f3fc118a99ebd36b3c`.

## Findings
- M00.9–M00.11 code is present in `main`.
- M01.1–M01.3 code/tooling is present in `main`.
- `.ai/STATE.md` and current tracking were behind the physical code state.
- No combined CI statuses were exposed for the audited head through the available connector surface, and the Actions run-list endpoint was not available through the connector allowlist.
- Therefore this synchronization deliberately does not invent or infer GitHub Actions success.

## Cleanup actually applied to the candidate tree
Removed 23 versioned transport/generated files:
- 4 root transport files: `APPLY.md`, `COMPLETE_PACKAGE_MANIFEST.md`, `FILES.sha256`, `PATCH_BASE.txt`;
- 7 generated files from `experiments/architecture-closure-poc/dist/`;
- 5 generated ZIP/checksum files from `experiments/export-target-poc/artifacts/`;
- 7 generated top-level files from `experiments/export-target-poc/dist/`.

`experiments/data-source-poc/dist/` and `experiments/export-target-poc/dist/targets/` were checked on current `main` and were already absent, so no deletion is claimed for them.

Source code, tests, workflows, fixtures, ADRs and `.ai/evidence/*` remain versioned. `.gitignore` now prevents root transport files and generated `experiments/*/dist` / `experiments/*/artifacts` outputs from returning.

## Tracking preservation
The previous `.ai/TRACKING.md` blob is preserved byte-for-byte as `.ai/TRACKING_HISTORY_THROUGH_M00.8.md`. The new `.ai/TRACKING.md` is a current status/index and records the exact required closure chain.

## Formal state
- Last formally verified historical closure: M00.8.
- Code prepared through M01.3.
- Formal gate: `CI_GATES_PENDING`.
- M01.4 remains blocked until M00.9 → M01.3 evidence is verified and recorded.
