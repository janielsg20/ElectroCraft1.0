# Cierre M02.5 — 2026-08-18

## Resultado
M02.5 — Definir Theme, Blueprint, Registries y Capability ownership: `COMPLETADA`.

## Integración
- PR: `#7`.
- Squash merge en `main`: `7cb7016d6ae6e4e91cef3a7de41de66cb861fc55`.
- Contratos canónicos permanecen bajo `packages/domain/src/contracts/`; no se creó un package paralelo.

## Contrato implementado
- `ElectroCraftTheme` contiene exclusivamente diseño visual portable: tokens, typography, variants, spacing, radius, shadows y motion.
- Template continúa como `ElectroCraftDocument kind=template` con `templateMeta` y Display Conditions; no existe un segundo árbol/template model.
- `ElectroCraftBlueprintPackage` es externo/versionado; describe artifacts y required capabilities sin convertirse en fuente de verdad persistida del proyecto.
- `ElectroCraftBlueprintInstaller` vive en application y soporta plan, detección de conflictos, apply y rollback con journal verificable.
- `ElectroPlatformCapabilityRegistry` y los registries Component/Field/Action/Provider viven en application y no se serializan completos en ProjectDefinition.
- `ElectroCraftProjectDefinition` evoluciona a schema v3 con `originBlueprint`, `requiredCapabilities`, `targetCapabilityOverrides` y `userRegistryDefinitionRefs`.
- `ElectroCraftDocument` evoluciona a schema v3 con `templateMeta`; migrations explícitas desde v2/v1/page permanecen fail-closed.
- Solo definitions `origin=user` son elegibles para persistencia por ref; core/extension permanecen en registries de aplicación.
- `export-ir` consume un reporte neutral `supported | adapted | blocked`, no el registry runtime.
- Se preserva el invariant F01 de exactamente 17 owner packages; la referencia histórica `packages/contracts/` se materializa como `packages/domain/src/contracts/`.

## Evidencia funcional
- Blueprint: instalación válida, conflicto real `create-object-exists` y rollback confirmado.
- Capability analyzer: `capabilityEntries=9`, con cobertura real `supported`, `adapted` y `blocked`.
- Export capability plan neutral generado desde el reporte canónico.
- Project/Document v2 migran explícitamente a v3 sin inventar registries ni provenance.

## Pruebas
- Suite dedicada M02.5: `14/14` verdes.
- Suite acumulada en `main`: Node `27/27`, Vitest `83/83`, Playwright `1/1`.
- Prettier/lint, TypeScript typecheck, package boundaries, Studio/PWA build y build raíz: verdes.
- Gates heredados M02.1, M02.2, M02.3 y M02.4 permanecieron verdes con schema v3.
- Export parity heredado: static, Capacitor, LAMP y WordPress runtime verdes.

## CI final
- Gate PR final: run `32182296286` — `success`.
- Gate `main`: run `32182633428` — `success`.
- Marker: `PASS_M02_5_THEME_BLUEPRINT_REGISTRY_CAPABILITY`.
- Artifact final: `9341317925` — `m02-5-theme-blueprint-registry-capability-evidence`.
- Digest: `sha256:13d612fd3c8ae4af6cb88b52e5f3b1ce6d56d77e844b38f6625d959638a7f026`.
- Head de evidencia: `7cb7016d6ae6e4e91cef3a7de41de66cb861fc55`.

## Blockers
- P0: `0`.
- P1: `0`.

## Siguiente microfase
`M02.6 — Serializer y migrations de proyecto`.
