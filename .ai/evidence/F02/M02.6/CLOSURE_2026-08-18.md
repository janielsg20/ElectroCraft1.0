# Cierre M02.6 — 2026-08-18

## Resultado
M02.6 — Serializer y migrations de proyecto: `COMPLETADA`.

## Integración
- PR: `#8`.
- Squash merge en `main`: `c74fc8284a56487dc56b9dbb90775fb592f803d8`.
- Zod permanece como boundary owner y `packages/domain/src/contracts/` como ubicación canónica; no se creó un package paralelo.

## Contrato implementado
- Una única ruta `stableCanonicalStringify` ordena objetos/arrays de forma determinista y es reutilizada por serializers y snapshots.
- `ElectroCraftProjectSnapshotEnvelope` normaliza ProjectDefinition + Documents, ordena documentos por ID y fija `schemaVersion=1` del envelope.
- Checksum portable `fnv1a64` se calcula sobre el snapshot canónico sin depender del orden accidental de claves.
- `ElectroCraftMigrationRegistry` registra pasos secuenciales y fail-closed; ProjectDefinition usa los pasos reales v1→v2 y v2→v3.
- `migrateElectroCraftProjectDefinitionPayload` ya no mantiene una ruta inline paralela y puede migrar explícitamente a v2 o v3.
- `ProjectImportService` sigue la secuencia JSON → Zod → refs → canonical checksum → persistencia; no llama a `putMany` si existe cualquier diagnostic bloqueante.
- Diagnostics de schema/checksum son reparables y conservan path/code/reparación.
- `export-ir` recibe un manifest neutral del snapshot con revision/checksum/schema facts, no repository ni runtime internals.

## Evidencia funcional
- Snapshot canónico fijo conserva el checksum esperado `fnv1a64:62421025ba8927ee` aunque los documentos lleguen en distinto orden.
- Fixture real de migration v1→v2 verifica el estado intermedio, y v1→v3 aplica exactamente dos pasos registrados.
- Storage filesystem real confirma que checksum/schema inválidos no cambian ningún archivo.
- Reapertura de records v1 migra ProjectDefinition a v3 y reescribe storage mediante el registry.

## Pruebas
- Suite dedicada M02.6: `14/14` verdes.
- Suite acumulada en `main`: Node `27/27`, Vitest `97/97`, Playwright `1/1`.
- Prettier/lint, TypeScript typecheck, package boundaries, Studio/PWA build y build raíz: verdes.
- Gates heredados M02.1, M02.2, M02.3, M02.4 y M02.5 permanecieron verdes.
- Export parity heredado permaneció verde en static/Capacitor/LAMP/WordPress.

## CI final
- Gate `main`: run `32186495673` — `success`.
- Marker: `PASS_M02_6_SERIALIZER_MIGRATION_REGISTRY`.
- Artifact final: `9342646837` — `m02-6-serializer-migration-registry-evidence`.
- Digest: `sha256:35ce99bc90392494dd1d0e6a276a41ec245e4b62a849a6d6f59fb454404121cc`.
- Head de evidencia: `c74fc8284a56487dc56b9dbb90775fb592f803d8`.

## Blockers
- P0: `0`.
- P1: `0`.

## Siguiente microfase
`M02.7 — Definir ElectroCraftExportIR`.
