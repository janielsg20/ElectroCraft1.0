# M02.3 bootstrap trigger

Temporary marker used only to force a real feature-branch push so GitHub Actions can regenerate the root lockfile and apply the repository formatting contract. Remove before final validation and merge.
