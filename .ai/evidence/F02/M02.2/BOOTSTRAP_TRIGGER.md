# M02.2 bootstrap trigger

Temporary marker used only to force a real branch ref push so GitHub Actions can generate the reproducible lockfile and apply repository formatting before PR validation. Remove before final validation/merge.
