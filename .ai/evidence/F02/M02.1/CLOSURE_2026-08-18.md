# Cierre M02.1 — 2026-08-18

## Resultado
M02.1 — `ElectroCraftProjectDefinition` + `ElectroCraftDocument`: `COMPLETADA`.

## Integración
- PR `#3`: `Implement M02.1 canonical project and document model`.
- Squash merge a `main`: `cf4649d98f96a553daa020581a918d9559131137`.
- Sin force push.

## Contrato implementado
- `ElectroCraftProjectDefinition` y `ElectroCraftDocument` versionados bajo `packages/domain/src/contracts/`.
- IDs deterministas namespaced.
- refs tipadas y validación semántica fail-closed.
- migración legacy exclusiva `page -> screen`.
- serialización canónica estable y round-trip.
- repository port + `ProjectDocumentService` para save/reopen/recovery.
- Zod `4.4.3` fijado por lockfile como validation boundary.
- No se creó `packages/contracts`: el owner congelado por F01 sigue siendo `packages/domain`.

## Pruebas
- Unit: IDs, round-trip, strict schemas, legacy migration.
- Contract: ProjectDefinition referencia documentos en vez de embebidos; missing refs; rechazo de Puck/AppState internals; domain sin imports de editor/storage/runtime.
- Integration: persist/save/reopen/recovery y migración de record legacy.
- Suite dedicada final: `12/12` tests verdes.
- Pipeline raíz: lint, typecheck, boundaries, node tests, Vitest, build, Studio PWA y Playwright verdes.

## CI final sobre main
- Workflow M02.1: run `32167600544` — `success`.
- Marker: `PASS_M02_1_PROJECT_DOCUMENT_MODEL`.
- Artifact: `9336023224` — `m02-1-canonical-model-evidence`.
- Digest: `sha256:1007752a6368d124818bbfbfffac199eac91629227b93c77b632da3987074fcb`.
- `electrocraft/M01.4`: `success`.
- `electrocraft/M00.9`: `success`.
- `electrocraft/M00.10`: `success`, incluida matriz real Static/Capacitor/Slim-PDO-CSRF/WordPress.

## Fallos detectados y corregidos antes del cierre
1. Prettier detectó 9 archivos nuevos fuera del estilo del repo: se aplicó exclusivamente el formato canónico.
2. El boundary test confundía el target canónico `react` con un import React: se cambió a detección de imports prohibidos.
3. El gate heredado F01 exigía domain sin ningún import; se adaptó para permitir Zod y módulos relativos del propio domain, manteniendo prohibidos packages/frameworks/runtime/adapters.

## Blockers
- P0: `0`.
- P1: `0`.

## Siguiente microfase
`M02.2 — Definir Component/Layout/Style`.
