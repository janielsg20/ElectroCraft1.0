# Cierre M04.6 — Import/Backup/Restore

Estado: `COMPLETADA / GREEN`.

Backup versionado contiene manifest, snapshot canónico, media refs y checksum. Import valida formato/version/checksum antes de escribir, ofrece copy/replace explícito y restore crea checkpoint `pre-restore`. UI española permite descargar y seleccionar archivo.
