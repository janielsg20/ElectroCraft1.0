# F00 / M00.8 evidence — Gemini code generation

State: `COMPLETED / GREEN`.

## Product scope frozen
Gemini is used by ElectroCraft to generate/refine **code for components, plugins and sections**. Image generation is explicitly outside M00.8.

## Reproducibility
- Exact pins: `ai@7.0.48`, `@ai-sdk/google@4.0.31`, `@google/genai@2.15.0`, `zod@4.4.3`, `typescript@6.0.3`.
- Deterministic `package-lock.json` committed; all final CI installs use `npm ci`.
- Final head: `9f732e1715da3f6b953dec05223d22b2773b3225`.

## Final GitHub Actions evidence
Run `32088311808` -> SUCCESS.

Jobs:
- lockfile-bootstrap `95565311706` -> SUCCESS;
- verify-static `95565335277` -> SUCCESS;
- live-gemini `95565379219` -> SUCCESS;
- report-status `95565541151` -> SUCCESS.

Static gate:
- registry health -> PASS;
- committed lockfile / `npm ci` -> PASS;
- exact versions -> PASS;
- lint -> PASS;
- strict TypeScript -> PASS;
- build -> PASS;
- tests -> PASS;
- real package API contract -> PASS;
- gateway/security scan -> PASS;
- static closure -> PASS.

Live gate:
- structured code plan -> PASS;
- real `CodeArtifactPoc` -> PASS;
- bounded tool loop -> PASS;
- streaming code -> PASS;
- cancellation fail-closed -> PASS;
- Gemini Interactions `v1` stateless code probe -> PASS;
- `PASS_LIVE_GEMINI_CODE` -> PASS;
- `PASS_LIVE_CLOSURE_GATE` -> PASS.

Generated-code evidence from final run:
- artifact type `component`;
- 1 TSX file;
- entry `./StatusBadge.tsx`;
- 1609 generated bytes;
- SHA-256 `6805458a7e430a6ce49c664397b4514ed2ec325adb5a7c3e23ed8c6515cb6d18`;
- plan had 3 steps and requested only read/draft/validate tools;
- real tool loop executed `get_app_summary` only and stopped in 2 steps.

## Security evidence
- API key is GitHub Actions/server-only and masked in logs.
- client contract has no API-key/provider fields.
- generated artifacts reject traversal/absolute paths, duplicate paths, invalid entry file, secret references and `draftOnly=false`.
- model tools cannot Apply, write DB/SQL/files, execute arbitrary JS, install packages/extensions, deploy, delete projects or access secrets.
- Interactions probe uses `store:false`; M00.8 does not require server-side conversation persistence.

## Artifacts
- live evidence `9307469682`, digest `sha256:4f85501c817461cdd1964f4dcc5ce06886fc32ef3be571e80a1757dbf1a32694`;
- static evidence `9307452584`, digest `sha256:624c5ee51052e1de73877ee68e940bdcc421dce8d1aee8298a703266298819e6`.

## Closure decision
M00.8 is GREEN. M00.9 may start. No image-generation or image-model requirement carries forward from this microphase.
