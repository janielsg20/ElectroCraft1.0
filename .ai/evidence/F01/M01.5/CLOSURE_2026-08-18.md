# M01.5 — Formal Closure Evidence — 2026-08-18

## Scope
M01.5 creates the reproducible GitHub Actions base CI required by F01. It introduces the root npm lockfile, locked installation, safe dependency caching, explicit quality gates, CI contract verification, and evidence artifacts without deployment or cloud secrets.

## Implementation
- Squash merge on `main`: `9fb64b7890ae92660d8d494d706245e800782e86`.
- PR validation head: `0a4a5f46758cbed23ead055dfab3a636884ead63`.
- The PR head and squash-merged main commit share the exact tree SHA `23bc994e19902830148650601656492a17dc7824`; therefore the successful PR Base CI validated the same source tree that was merged.
- Root version: `0.0.0-m01.5`.
- Package manager contract: `npm@10.9.2` on Node `22.16.0`.
- Root `package-lock.json`: lockfile version 3.
- `.npmrc`: `legacy-peer-deps=true` so locked installs use the same peer-resolution policy used to create the lockfile.

## CI contract
`.github/workflows/ci.yml`:
- triggers on `push` to `main` and on `pull_request`;
- permissions are `contents: read`;
- checkout uses `persist-credentials: false`;
- dependency cache is the npm cache keyed by `package-lock.json` through `actions/setup-node`;
- installation is `npm ci --ignore-scripts --no-audit --no-fund`;
- explicit gates: lint, typecheck, tests, build, Playwright, empty-repository fixture, CI artifact verification;
- no deploy, publish, Cloudflare/Vercel command, deployment permission, or cloud secret is present.

## ElectroCraft verification added
- `tooling/src/ci-base.mjs` evaluates the CI contract fail-closed.
- `tooling/scripts/verify-ci-base.mjs` writes `tooling/dist/m01-5-ci-base-report.json`.
- `tooling/test/ci-base.test.mjs` covers ready, unlocked/secret negative, and stale-lock negative cases.
- `tooling/vitest/contract/ci-base-workflow.test.ts` validates the real workflow contract and absence of forbidden deep-source/deploy/secret patterns.
- `tooling/vitest/integration/ci-base-report.test.ts` validates the real report produced by the repository build.
- `help.architecture.repository` documents the locked CI architecture in Spanish.

## Bootstrap adaptation
The first lockfile-generation attempt, run `32157236793`, failed inside npm/Arborist with `Cannot read properties of null (reading 'edgesOut')`. No ElectroCraft quality gate was disabled or bypassed. The lockfile was regenerated using `legacy-peer-deps`, and the same setting was committed in `.npmrc` so `npm ci` reproduces that resolution policy instead of silently changing dependency semantics.

## Dedicated M01.5 evidence
- Base CI PR run: `32158198590`.
- Validation head: `0a4a5f46758cbed23ead055dfab3a636884ead63`.
- Exact validated tree: `23bc994e19902830148650601656492a17dc7824`.
- Job `Locked lint typecheck test build`: `success`.
- Successful steps: locked toolchain verification, `npm ci`, lint, typecheck, tests, build, Playwright, empty-repo fixture, CI report verification, closure marker and evidence upload.
- Marker: `PASS_M01_5_BASE_CI`.
- Artifact: `9332533012` — `m01-5-base-ci-evidence`.
- Artifact digest: `sha256:439631825171a8316e5b850051ea63f3eb693f3235a8cd71e803a6a04afe5758`.

## Revalidation on merged main tree
Merged head: `9fb64b7890ae92660d8d494d706245e800782e86`.

- M01.4 — `success`; run `32158500101`, including full repository pipeline, PWA artifact verification and closure marker.
- M00.9 — `success`; run `32158500161`.
- M00.10 — `success`; run `32158500161`, including static parity, Capacitor, LAMP/MySQL/PDO/CSRF, WordPress wp-env and closure gate.
- M00.11 — `success`; run `32158786503`.
- M01.1 — `success`; run `32158875745`.
- M01.2 — `success`; run `32158875745`.
- M01.3 — `success`; run `32158875745`.

M01.5 satisfies `DONE requires evidence` and is formally `COMPLETADA`.

## Continuation
The next exact microphase is M01.6 — **Documentar conventions**, following `.ai/microphases/M01_6.md`.
