# M01.4 — Formal Closure Evidence — 2026-08-18

## Scope
M01.4 creates the real React/Vite TypeScript Studio bootstrap, a technical PWA shell, the temporary Project Home route `/`, fail-closed bootstrap health, and the repository gates required by `.ai/microphases/M01_4.md`.

## Implementation history
- Initial M01.4 implementation commit: `e3f8ef23de22918f27d68a69e188fa9daf09553e`.
- Formatting correction: `c91eeb43c8f73960b6f55d1b62c03d3799bd5d2a`.
- Vite client type declaration correction: `e0a73dd0163e62c02aeebb79c2414f38e261d7cc`.
- M01.1 inherited closure compatibility correction for the new PWA artifact: `ed4e9f12486b3ddf4f3351517867a6ee9cd73a15`.

## Failures observed and fixed
1. Run `32145287163` exposed Prettier drift in `apps/studio/src/styles.css` and `tooling/vitest/contract/studio-import-boundary.test.ts`. The exact Prettier output was reproduced in a temporary diagnostic workflow and applied without logic changes.
2. Run `32146232918` passed formatting and exposed TypeScript `TS2882` for the Studio CSS side-effect import. `apps/studio/src/vite-env.d.ts` now references `vite/client`; typechecking remains enabled and strict.
3. Inherited M01.1 revalidation exposed a stale closure assertion for the pre-M01.4 `apps/studio/dist/studio-architecture.js` library artifact. The M01.1 closure now validates the real PWA outputs (`index.html`, `manifest.webmanifest`, `sw.js`) and `m01-4-studio-bootstrap-report.json`.

## Green M01.4 evidence
- GitHub Actions run: `32146389103`.
- Head: `e0a73dd0163e62c02aeebb79c2414f38e261d7cc`.
- Job: `M01.4 React/Vite/PWA bootstrap` — `success`.
- Commit status: `electrocraft/M01.4 = success`.
- Closure marker: `PASS_M01_4_STUDIO_BOOTSTRAP`.
- Evidence artifact: `9327946718` — `m01-4-studio-bootstrap-evidence`.
- Artifact digest: `sha256:294db22929694beea2197dedf20f2acb9d9d8f241dd655a972d2d67bcdfa332a`.

## Gates proven green in the M01.4 run
- exact dependency installation and engine pins;
- Prettier/lint;
- TypeScript typecheck;
- workspace/import boundaries;
- Node unit/contract/negative tests;
- Vitest unit/contract/integration projects;
- real Vite production build;
- PWA artifact verification;
- Playwright artifact QA;
- closure marker and uploaded evidence artifact.

## Artifact contract after M01.4
The Studio is now an application/PWA build, not the temporary M01.1 library artifact. Its required build evidence is:
- `apps/studio/dist/index.html`;
- `apps/studio/dist/manifest.webmanifest`;
- `apps/studio/dist/sw.js`;
- `tooling/dist/m01-4-studio-bootstrap-report.json`.

## Inherited-chain revalidation on the compatibility head
Compatibility head: `ed4e9f12486b3ddf4f3351517867a6ee9cd73a15`.

- M00.9 — `success`; run `32146926071`.
- M00.10 — `success`; run `32146926071`, including static parity, Capacitor, LAMP/MySQL/PDO/CSRF, WordPress wp-env and closure gate.
- M00.11 — `success`; run `32147304063`.
- M01.1 — `success`; run `32147392932`, including the updated PWA artifact closure marker.
- M01.2 — `success`; run `32147392932`.
- M01.3 — `success`; run `32147392932`.

All inherited gates through M01.3 are green after the Studio artifact transition. The M01.4 dedicated gate is also green with its own artifact evidence. Therefore M01.4 meets the repository rule `DONE requires evidence` and is formally `COMPLETADA`.

## Continuation
The next exact microphase is M01.5 — **Crear CI base**, following `.ai/microphases/M01_5.md`.
