# M08.12 — Cierre certificado — 2026-09-06

## Resultado

`GREEN / MERGED`.

## Candidate certificado

- Rama: `codex/m08-12-record-crud-validation`
- Candidate: `8b9371756006a754bbbf1702cff963369eeeabd6`
- ElectroCraft Base CI: run `34063642245` (#914)
- Resultado: `success`
- PR: `#79`
- Merge squash a `main`: `096fb2bc6ae7110c899968b851728e0fa5795e96`

## Capacidad cerrada

M08.12 entrega CRUD de Registros validado sobre el owner `PGlite generic content store` existente:

- validación create/update contra `ElectroCraftDataSchema`;
- una sola tabla física `content_records`;
- soft delete mediante `state=deleted` + `deletedAt`;
- queries excluyen eliminados por defecto y `includeDeleted` es explícito;
- update normal no revive registros eliminados;
- `restrict`, `detach` y `cascade` conservan atomicidad;
- `/content` expone selector de modelo, list/detail, formulario generado y vista de eliminados;
- mutaciones Studio pasan por `dataSourceWorkspaceRuntime -> ConnectorRegistry -> InternalDataSourceAdapter`.

## Corrección final antes del gate

El E2E previo detectó una carrera real cuando `Incluir eliminados` podía cambiar durante el refresh posterior al soft-delete. El candidate certificado serializa esas mutaciones y bloquea temporalmente controles que podían iniciar un refresh concurrente. No se relajó la prueba.

## Continuidad

Con M08.12 fusionada y certificada, la siguiente microfase exacta es `M08.13 — Índice tipado para búsqueda/filtros`.
