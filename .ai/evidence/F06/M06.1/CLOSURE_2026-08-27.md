# M06.1 — Closure evidence — 2026-08-27

Estado: `COMPLETADA / GREEN`.

## Resultado observable

- `ElectroCraftDocument` schema v4 persiste `layout` y `style` opcionales en cada nodo.
- La importación v3 migra recursivamente a v4; schemas estrictos y tests negativos rechazan CSS/payloads no canónicos.
- `@electrocraft/editor-puck` proyecta Layout/Style a props reservadas de transporte y los reconstruye sin filtrar internals Puck al documento.
- `usePuckEditorPresentation()` usa `api.selectedItem`, `getSelectorForId` y el action público `replace`; no existe store de selección/historial paralelo.
- `Inspector > Diseño > Avanzado` ofrece tabs Diseño/Estilo, presets Columna/Fila/Cuadrícula/Envolver, gap/alineación/columnas, tokens de relleno/fondo, opacidad y reset/herencia.
- Los renderers core aplican la semántica al Canvas; el CSS generado es una proyección Studio-only, no source of truth.
- La ayuda `help.editor.advanced` es persistente y la UI conserva Progressive Disclosure, labels, keyboard y touch targets de 44 px.

## Archivos principales

- `packages/domain/src/contracts/document.ts`
- `packages/domain/src/contracts/component-definition.ts`
- `packages/editor-puck/src/puck-layout-style.ts`
- `packages/editor-puck/src/puck-document-adapter.ts`
- `packages/editor-puck/src/puck-editor-composition.ts`
- `apps/studio/src/features/editor/advanced/layout-style-inspector.tsx`
- `apps/studio/src/features/editor/advanced/layout-style-inspector.css`
- `apps/studio/src/features/editor/advanced/presentation-style.ts`
- `apps/studio/src/features/editor/puck-core-components.tsx`
- `apps/studio/src/shell/editor-workspace.tsx`

## Evidencia de tests y gates

- `npm.cmd run lint`: GREEN.
- `npm.cmd run lint:offline`: GREEN.
- `npm.cmd run typecheck`: GREEN.
- `npm.cmd run test:docs`: GREEN; una sola microfase activa.
- `npm.cmd run test:node`: 41/41 GREEN.
- `npx.cmd vitest run --config vitest.config.ts --maxWorkers=1 --no-file-parallelism`: 116/116 archivos, 415/415 tests GREEN.
- Tests M06.1: unit `layout-style-inspector`, contract `layout-style-inspector-boundary`, integration `layout-style-puck-roundtrip` y casos de migración/adapter existentes GREEN.
- `npx.cmd playwright test --config playwright.config.ts tooling/playwright/m06-1-layout-style-inspector.spec.ts`: 1/1 GREEN; inserción, selección Puck, Diseño/Estilo, Canvas, undo/redo, autosave y reopen.
- `npm.cmd run build`: GREEN; workspace/boundaries/quality/CI/docs, Vite Studio y PWA artifact.
- `git diff --check`: GREEN.

## Adaptaciones y blockers

- La primera suite Vitest paralela agotó timeouts de PGlite por contención de CPU/disco durante una descarga de Chromium. Los casos afectados pasaron al repetirlos en serie y la suite completa serial quedó 415/415.
- `agent-browser` no consiguió iniciar después de su instalación local. La verificación de navegador se realizó con el Chromium exacto de Playwright y el flujo E2E real quedó GREEN.
- Warnings conocidos del build: `eval` y chunk size provenientes de PGlite; no son errores ni una regresión M06.1.
- Blockers funcionales P0/P1: `0`.

## Continuidad

Siguiente microfase exacta: `M06.2 — Responsive inheritance y reset`.
